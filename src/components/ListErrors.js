import React, { PureComponent } from 'react'
import PropTypes from 'prop-types'

export default class Listerrors extends PureComponent {
    static propTypes = {

    }

    render() {
        return (
            <>
                
            </>
        )
    }
}
